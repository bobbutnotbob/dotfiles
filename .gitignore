# Ignore whatever wacky business sublime is spewing out
config/sublime-text/Local
# We also don't want the autogenerated PyWal stuff
config/sublime-text/Packages/User/PyWal.sublime-color-scheme

# Ignore all logs as well bc we don't want those
**/Logs

# Zsh is spitting out some some stuff we don't want
zsh/.zcompdump*

# A folder for all the stuff we're going to download from GitHub
# (just so I'm not committing too much of other people's stuff)
gitgrabs
